Family: long the next day when BTC perpetual taker sell volume dominates its own trailing bottom quintile.
Frozen 2026-09-25 01:44:16 UTC, before any taker-ratio value or next-day return of this rule was computed.
Not the long/short-account rule: that rule read only count_long_short_ratio over 2022-2023 and was killed.
This column was not read there. Open interest is not read. Top-trader ratios are not read.
Not the funding rule frozen beside this file. Not a liquidation-cascade bid.

Spot cannot short. Long or flat. Books: BTC and ETH, both driven by one BTCUSDT futures series.
Series: data.binance.vision futures/um daily metrics for BTCUSDT.
Column locked here, before any file is opened: sum_taker_long_short_vol_ratio.
The day's value is the last row of that UTC date. A date with no row is missing.
Sort the previous 90 printed daily values ascending. The threshold is the 18th (1-based), index 17.
Long the next UTC day when date D's value is strictly below that threshold. Otherwise flat.
The 90-day window and the quintile are copied from the killed account-ratio rule so this test is not a looser bar.
The screen year is not that rule's sample.
Execution days are 2024-01-01 through 2024-12-31 whose previous calendar day has a value and 90 earlier values.
Prices are Binance spot daily closes (BTCUSDT, ETHUSDT). The long day earns that day's close over the previous close.
Cost 20 bps on each change from flat to long and 20 bps on each change from long to flat.
A position still long on the last screen day is charged the exit that day and earns no further return.
Stress doubles each charge. A loss kills the family. A gain is not a pass: these are daily closes, not prints through a price.
Screen only: 2024. No other year is scored. If the locked column is absent, record the gap and do not pick another column.
Null: the same number of long days, random execution days in the screen, 500 draws, seed 20260925.
Kill unless pooled equal-weight P&L > 0, stress > 0, round trips >= 60, both books > 0,
P&L > null p95, no calendar month is more than 40% of pooled P&L, and pooled P&L exceeds 400 bps.
Round trip = an entry from flat to long.
Do not flip the sign to high taker-buy ratio. Do not change the quintile. Do not add 2025.
