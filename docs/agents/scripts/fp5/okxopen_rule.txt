Family: the OKX-minus-Binance BTC basis, same signal as the killed close-fill rule, with the fill changed before this text is hashed.
Frozen 2026-09-25 06:04:25 UTC, before any open of this rule was joined to a return.
Not a new signal. Not a flip. Not Coinbase. Not the BTC perpetual premium index. Not the coin-margined versus linear premium. Not OKX volume.
Spot cannot short. Long or flat. Books: BTC and ETH, both driven by this cross-exchange basis.
OKX history candles, instId BTC-USDT, bar 1Dutc. The row is [ts, open, high, low, close, vol, volCcy, volCcyQuote, confirm]. Close is index 4. A row whose confirm is not 1 is not a level. Timestamp is unix milliseconds, UTC.
Binance close is field index 4 (0-based) of the BTCUSDT spot 1d kline.
The level is okx_close / binance_close - 1. A level is missing unless both closes are positive. The basis is signed, so the change is a difference.
Change = level[D] - level[previous calendar day]. Missing unless the previous calendar day has a level.
Sort the previous 90 printed changes ascending. The threshold is the 72nd (1-based), index 71.
The signal fires when that change is strictly above the threshold. The execution day is the next UTC calendar day.
A day whose previous calendar day has no change is not an execution day. The signal is not carried forward.
OKX BTC-USDT 1Dutc starts 2018-01-11, so 2019 is the first January with 90 earlier changes.
Execution days are 2019-01-01 through 2019-12-31. No other year is scored. No 2020 signal is scored.
Fill, decided before the result: enter at the open of the execution day and exit at the open of the next calendar day.
The open is field index 1 (0-based) of the Binance spot 1d kline for that book (BTCUSDT, ETHUSDT). Binance prints a daily bar every calendar day, so that open is the next tradable open. There is no US-session filter.
Gross bps on a book = (exit open / entry open - 1) * 10000. A trip exists only when both opens are positive. A signal missing either open is not a trip.
Each signal is its own round trip. The position is not carried. If the next day is also a signal, the first trip exits at that day's open and the second trip enters at the same open. Both the exit and the new entry are charged. There is no netting.
Cost 20 bps on the entry and 20 bps on the exit. Stress doubles each charge to 40 and 40.
Both charges are booked on the entry day's calendar month. The exit open on 2020-01-01 is allowed for an entry on 2019-12-31. It is a fill, not an extra scored year.
Null: the same number of trips, random execution days in 2019 that have both opens, 500 draws, seed 20260925, using these open-to-open returns and the same two charges. Not close-to-close.
Kill unless pooled equal-weight P&L > 0, stress > 0, round trips >= 60, both books > 0,
P&L > null p95, no calendar month is more than 40% of pooled P&L, and pooled P&L exceeds 400 bps.
Round trip = one entry at an execution-day open and the exit at the next day's open.
A gain on these opens is not a testing row: the open is a Binance daily-kline open, not a print through the Revolut X book. Do not arm. Do not edit PR5.
Do not flip the sign to a narrowing basis. Do not go back to close-to-close. Do not switch venue. Do not change the quintile. Do not add 2020.
