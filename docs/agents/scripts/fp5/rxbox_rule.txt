Family: long the next UK-book open when both the open and the close of the completed BTC bar sit strictly inside the previous BTC high and low, and at least one of the open or the close of the completed ETH bar sits strictly outside the previous ETH high and low. Touching an extreme does not count as inside and does not count as outside. This is not the close-only inside rule and not the open-only inside rule.
Frozen before any open of this rule was joined to a return.
Public file: the same Revolut X UK pull as pass 62, docs/agents/backtests/inputs/fp5_2026-09-25/pass62/revx/{BTC-USD,ETH-USD}_240.json. Every stored request has region=UK. The account trades the UK book. An EEA candle is not read. BTC-GBP is not stood in for ETH. Another pair is not stood in for either book. Bars run from 2025-09-11 00:00 UTC through the bar that starts 2026-09-22 20:00 UTC. 2,262 bars on each book. Consecutive starts are exactly four hours apart. A missing start is not filled. A duplicate start is unusable.
The open is the first trade of that four-hour bar. The high and the low are trades inside that bar. The close is the last trade. A bar is kept only when its volume is strictly above zero. This pull has no zero-volume bar. A zero-volume open is not a fill. Volume is not the signal. This is not a Binance daily open. This is not the 29-day minute tape. That tape has no ETH-USD. The 60-trip gate is not lowered because of it.
The pass 62 comparisons of first trades and last trades are not rerun, and their signs are not flipped. The pass 63 comparisons of ranges, seating, both ends, inside prints, and the previous midpoint are not rerun, and their signs are not flipped. A whole bar strictly above the previous last trade, with the other book strictly below, had no days either way and is not a rule. A high that clears the previous high while the low stays inside that range, with the other book doing the opposite, is the same set of bars as that both-ends rule on this tape. It is not a new rule and it is not scored. This is not an on-chain series and not a calendar sequence. Fifteen-minute bars are not read. Six-hour bars are not read. Same-day rankings of which kline is higher are not rerun. The order of six-hour closes is not rerun. How four-hour ranges sit inside one another on one coin is not rerun. Median confirmation time is not rerun. Transaction fees are not rerun. Coin-margined mark closes are not rerun. Not funding. Not the OKX basis. Not an inside bar and not an engulfing bar. Not a trailing quintile. Not a one-percent open gap. Not an N-day return gap.
The signal uses only bars that have already closed. Bar 0 is the completed bar. Bar 1 is the previous bar. An equal price does not fire. Do not reverse this. The pre-specified backup was already interior, so it is not frozen and it is not this rule. A comparison with zero days is not a rule.
Spot cannot short. Long or flat. Books: BTC-USD and ETH-USD on the UK book, both driven by this one comparison.
The signal is known when bar 0 closes. The execution bar is the next four-hour bar. The signal is not carried forward.
Fill, decided before the result: enter at the open of the execution bar and exit at the open of the following four-hour bar, on each book. Both opens must come from bars whose volume is strictly above zero. Do not replace either open with a Binance open. Do not use an EEA open.
Gross bps on a book = (exit open / entry open - 1) * 10000. A trip exists only when both opens are positive and both bars have volume strictly above zero.
Each signal is its own round trip. The position is not carried. If the next bar is also a signal, the first trip exits at that bar's open and the second trip enters at the same open. Both the exit and the new entry are charged. There is no netting.
Cost 20 bps on the entry and 20 bps on the exit. Stress doubles each charge to 40 and 40. Neither charge is lowered.
Both charges are booked on the entry bar's calendar month, UTC.
Null: the same number of trips, random execution bars that have both opens, 500 draws, seed 20260925, index int(0.95*500)-1. The null is not loosened.
Kill unless pooled equal-weight P&L > 0, stress > 0, round trips >= 60, both books > 0,
P&L > null p95, no calendar month is more than 40% of pooled P&L, and pooled P&L exceeds 400 bps.
Round trip = one entry at an execution-bar open and the exit at the next bar's open.
The reported figures are that arithmetic. A numeric clear on these UK opens is still not a testing row unless an order of this account was shown crossing the book. No such print is in this screen. It is not paper testing. Do not arm. Do not edit PR5. The five earlier numeric clears and the confirmation-time fall after a higher day stay void.
Do not lower the 60-trip gate to 44, to 10, to 15, or to fit this screen. Do not flip a sign. Do not build a testing row.
Counted before any return was joined, out of 2,258 bars that have two earlier bars and two later bars: 230 of 2,258. ETH's open and close both strictly inside, with BTC's open or close strictly outside, counted 312 and was not frozen.
