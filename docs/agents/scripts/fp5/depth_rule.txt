Family: long the next day when BTC perpetual bid depth outweighed ask depth at the 1% band.
Frozen 2026-09-25 02:06:04 UTC, before any book-depth notional or next-day return of this rule was computed.
Not the Revolut queue (that book has no history and stays untestable).
Not the futures taker ratio. Not open interest. Not funding.
The series is resting notional on Binance's published bookDepth, bid over ask, at one band.

Spot cannot short. Long or flat. Books: BTC and ETH, both driven by one BTCUSDT futures book.
Series: data.binance.vision futures/um daily bookDepth for BTCUSDT.
Columns locked here, before any file of this rule is opened: timestamp, percentage, notional.
The band is percentage -1 over percentage 1. No other band is read. The depth column is not read.
The day's snapshot is the last timestamp of that UTC date. The ratio is that snapshot's -1 notional divided by its 1 notional.
A ratio is missing unless both notionals exist and the ask notional is positive. A missing file is a missing day.
Sort the previous 90 printed ratios ascending. The threshold is the 72nd (1-based), index 71.
Long the next UTC day when date D's ratio is strictly above that threshold. Otherwise flat.
Execution days are 2024-01-01 through 2024-12-31 whose previous calendar day has a ratio and 90 earlier ratios.
Prices are Binance spot daily closes (BTCUSDT, ETHUSDT). The long day earns that day's close over the previous close.
Cost 20 bps on each change from flat to long and 20 bps on each change from long to flat.
A position still long on the last screen day is charged the exit that day and earns no further return.
Stress doubles each charge. A loss kills the family. A gain is not a pass: these are daily closes, not prints through a price.
Screen only: 2024. No other year is scored. If the locked columns or the ±1 band are absent, record the gap and do not pick another column or band.
Null: the same number of long days, random execution days in the screen, 500 draws, seed 20260925.
Kill unless pooled equal-weight P&L > 0, stress > 0, round trips >= 60, both books > 0,
P&L > null p95, no calendar month is more than 40% of pooled P&L, and pooled P&L exceeds 400 bps.
Round trip = an entry from flat to long.
Do not flip the sign to an offer-heavy book. Do not change the quintile. Do not add 2025. Do not change the band.
