# fp84 screen: the quote-weighted hourly return fails (2026-09-25)

The protocol was committed before this run (`409b715`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp84/screen_2023.json`, sha256
`9c6091a31cc4ec76adb3efa7f691fab211059f525161a051a0ba1df01c6c54d7`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| WTD-RET | 35 | +21.85 bps | +78.43 bps | +$7.65 | no |

The count clears. The file's gross is +41.9138 bps and the file's mean is
+21.85 bps. The file's null is +78.4258 bps, the same figure as §3.75,
because both rules have 35 trades on the same BTC pool and the same seed.
The mean is positive and under the null. The total is +$7.6475. Every fill
matches fp5's `net_return` on the next BTC open. All 35 signal days have a
trade. The hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC.
That day is not a print. It was not filled in. 456 days have a return. The
daily grid has 458 bars and no hole. A lower weighted return, and the 80th,
are not scored. The rule is not fit to add.
