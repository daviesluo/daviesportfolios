# fp267 protocol: FASTER (2026-09-25)

Written before any return of this rule was computed. PICK's later years are
a research record and are not a testing row. The other trade was about one
cent ahead, the 2025-01-01 through 2026-09-16 total is near zero, and the
fill is not a price this account can get. PICK is not a Revolut X paper
candidate. LATER failed its later years and is void. The buys and the sells
of those two rules were not moved. fp257 through fp264 are scored. A filter
on an unconditional hold is closed. HI10 and STEP failed their later years.
CMF and QNEW failed their later years. TRIP and CFAL failed their later
years. MTH is void: its first null mixed hold lengths. BRKHI is void: the
buy used a high and a low that were not known yet. A nearby signal is not
substituted to force a count.

fp209 through fp216 failed the 2023 screen. Every gross was positive and
every mean was negative. Each of those trades was long one contract and
short another. Same-day two-leg spreads stay closed. A one-day long, a
one-day short, and an overnight long or short stay closed. Funding level,
funding range, and funding cash stay closed. Open interest, the taker
ratio, the basis and volume jumps stay closed. No percentile is used.

The entry uses only a print that has already happened. The fill is an open
that exists on the contract being traded. A missing open is not taken from
another book. The position is one leg. It is not a two-leg spread. The null
below is another trade, held for the same number of days. It is not this leg
with the condition turned off. The count stays 30. LS-FADE's 60-trade
reproduction, the control, doubled costs, and the rule that no month is
above 40% of the profit are not relaxed. A Binance daily open that clears a
numerical gate is not a Revolut X fill and is not a paper-testing candidate.

This screen is the only measurement this file allows. A pass is not an
adoption and is not a testing row. It earns nothing until a later window
passes every pre-registered bar and the fill is a price on that contract.
2023 is spent once this screen runs.

Nothing here changes a frozen spec, arms a row, or sends an order. Public
reads only. Ten basis points a side. One hundred dollars stands on the
one leg, so the dollar result is one hundred times that leg's net.

## Costs, window, null

Ten basis points a side on the one leg. Entries in `[2023-01-01, 2024-01-01)` UTC.
An exit may be the 2024-01-01 open when the hold reaches that day. That
open is not an entry and not a signal. A day with no print is not filled
in, and it is not borrowed from another contract. The null is 200 draws,
seed `20250925`, index 190, of the other trade named below. When that other
trade has the same number of fills, the cutoff is its mean. When it has more,
the cutoff is the house p95 of samples of this rule's count. A pass is at
least 30 trades, mean net above zero, and mean net above that null.

## What a pass becomes

The house bar used for LS-FADE stays as written: both later sub-windows
positive, above a fresh null, positive with doubled costs, at least 30
trades, no month above 40% of the profit and the rest positive, and more
than 4% a year on the $100 it locks. The 60-trade reproduction of LS-FADE
is not relaxed. 2023 is not part of that bar. If this rule fails, it is
dead. This file does not arm a testing row.

## The idea

**FASTER.** A finished three-day USDT decline was strictly faster than the three-day window that ended three days earlier. Sell the USDT book for eight days. The other trade sells it eight days later for another eight days. Both windows have to fill. The entry open is not an input. One leg.

A count of spans, taken before this freeze, with no profit computed: the rule has 118 spans and the other trade has 118. Fillable days were 358 on the rule book and 358 on the other book.
