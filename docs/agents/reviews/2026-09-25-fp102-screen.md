# fp102 screen: LTC quote over BTC quote fails (2026-09-25)

The protocol was committed before this run (`6b35a2c`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp102/screen_2023.json`, sha256
`e20a93c500b1f659dfa1a9099161e3fbb22d4c55df0fa9a9abbdd1a1b420c413`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| LTC-Q | 63 | −59.16 bps | +43.10 bps | −$37.27 | no |

The count cleared. The file's gross is −39.2585 bps and the file's mean is
−59.16 bps. The file's null is +43.1045 bps. The total is −$37.2708. The
null is LTC's own one-day holds, so it does not match a BTC screen. The mean
is negative. Every fill matches fp5's `net_return` on the next LTC open. All
63 signal days have a trade. The daily grid has 458 bars. The 2024-01-01
quote is stored as zero and is not a print, so 457 days have a ratio. No
hourly bar was read. ETH was not scored. A day LTC was quiet versus BTC, and
the 80th, are not scored. The sign is not flipped. The rule is not fit to add.
