# fp65 screen: ETH's range over BTC's range fails (2026-09-25)

The protocol was committed before this run (`51fa8e1`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp65/screen_2023.json`, sha256
`d820f378e24c56f959c61de79d7737751590d4ed0c0081ba989d608fd49ab6c2`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| ETH-SPAN | 37 | −10.78 bps | +74.56 bps | −$3.99 | no |

The count clears. The file's null is +74.5602 bps, the same figure as
§3.57, because both rules have 37 trades on the same BTC pool and the same
seed. The mean is negative and under the null. The gross move is +9.21
bps; the 10 bps-a-side fill leaves −10.78. Every fill matches fp5's
`net_return` on the next BTC open. All 37 signal days have a trade. Both
daily grids have 458 bars and no hole. A flat range is not a print, and the
2024-01-01 high and low are stored equal to that bar's open on both
symbols, so the window has 457 ratios. A day ETH's range did not run wide
relative to BTC, and the 80th, are not scored. The rule is not fit to add.
