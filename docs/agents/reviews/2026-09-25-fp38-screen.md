# fp38 screen: the second half minus the first half fails (2026-09-25)

The protocol was committed before this run (`ff4d925`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp38/screen_2023.json`, sha256
`cc0389ba39d440292cbff5763b2ab19795fd5c2cc6c8efb5f95bdfb109fab450`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| HALF-DIFF | 34 | +22.92 bps | +76.29 bps | +$7.79 | no |

The count clears and the mean is positive. It loses to the null. The gross
move is +42.99 bps; the 10 bps-a-side fill leaves +22.92. Every fill matches
fp5's `net_return` on the next BTC open. All 34 signal days have a trade.
The hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC, so that
day is not a print. It was not filled in. The daily grid has 458 bars and no
hole. 456 days have a difference. A day whose second half does not outrun
the first, and the 80th, are not scored. The rule is not fit to add.
