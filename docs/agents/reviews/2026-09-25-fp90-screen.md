# fp90 screen: the last hour's average trade fails (2026-09-25)

The protocol was committed before this run (`9614bb3`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp90/screen_2023.json`, sha256
`b4c5d6456662fb4446cecc86527ced819b7b81bd145a4c101c7c1e6dfdef75c5`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| LAST-SIZE | 31 | +18.96 bps | +71.83 bps | +$5.88 | no |

The count cleared. The file's gross is +39.0172 bps and the file's mean is
+18.9592 bps. The file's null is +71.8264 bps, the same figure as §3.44,
because both rules have 31 trades on the same BTC pool and the same seed.
The mean is positive and under the null. The total is +$5.8774. Every fill
matches fp5's `net_return` on the next BTC open. All 31 signal days have a
trade. The hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC.
That day is not a print. It was not filled in. 456 days have a ratio. The
daily grid has 458 bars and no hole. A smaller last-hour average trade, and
the 80th, are not scored. The rule is not fit to add.
