# fp54 screen: the quote-weighted hour passes 2023 (2026-09-25)

The protocol was committed before this run (`68b331f`). The one-element
quote-field tuple was fixed in `96b48c5` before any bar was pulled. Two runs
of `measure.py` wrote the same file. Result
`docs/agents/backtests/fp54/screen_2023.json`, sha256
`b3b2686dc6b9d935e76e645d00a892e3bc76ac1e503dab9e5422945b6c29044f`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| VOL-CLOCK | 38 | +66.65 bps | +65.90 bps | +$25.33 | yes, the screen only |

The count clears. The mean is above the null. The file's mean net is
+66.6518 bps and the total is +$25.3277. The null p95 is +65.8998 bps. The
gross move is +86.81 bps; the 10 bps-a-side fill leaves +66.65. Every fill
matches fp5's `net_return` on the next BTC open. All 38 signal days have a
trade. The hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC,
so that day is not a print. It was not filled in. The daily grid has 458
bars and no hole. 456 days have a clock. A 2023 pass is not adoption. The
later years are in `2026-09-25-fp54-clock-result.md`.
