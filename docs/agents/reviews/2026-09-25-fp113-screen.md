# fp113 screen: coin open interest over base volume fails (2026-09-25)

The protocol was committed before this run (`e291a05`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp113/screen_2023.json`, sha256
`0f46e79223d8eefa4bae85ec8a5498858bf54c3cc29935f3be41dfcfdf62bbcd`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| OITURN | 61 | −5.8815 bps | +53.0762 bps | −$3.5877 | no |

The count cleared. The file's gross is +14.1268 bps and the file's mean is
−5.8815 bps. The file's null is +53.0762 bps. The total is −$3.5877. The null
is BTC's own one-day holds. The same null appears on TRDMIX because that
screen also has 61 trades on the same BTC opens. The mean is negative. Every
fill matches fp5's `net_return`. 457 prints. The open is not an input. The
2024-01-01 base volume is stored as 0. 61 signal days, 61 trades. Dollar open
interest is not read. No funding print was read. No hourly price bar was
read. No alt quote was read. A day below the 90th is not scored. The rule is
not fit to add.
