# fp150 screen: the cross-book volume jump fails (2026-09-25)

The protocol was committed before this run (`2fb56f4`). Two runs of `measure.py` wrote the same file. Result `docs/agents/backtests/fp150/screen_2023.json`, sha256 `fb5ec9d10c55bd1bb1f0bd7ea9692ddd7def21bb4481979f63664bffde8545ef`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| UMCMJMP | 26 | +12.6302 bps | +81.2211 bps | +$3.2839 | no |

The file's gross is +32.6755 bps and the file's mean is +12.6302 bps. The file's null is +81.2211 bps. The total is +$3.2839. The null is BTC's own one-day holds. The mean is positive and under the null. 26 trades are under 30, so the screen fails. The count stays 30. 453 stored days and 451 prints. 2023-08-28 through 2023-08-31 are absent and were not filled in. The ratio's level was not the statistic. The contract count was not stored. The window stays 90 days and 90 prints. Every fill matches fp5's `net_return`. No point in 2024 was stored as a signal. 26 signal days, 26 trades. The rule is not fit to add.
