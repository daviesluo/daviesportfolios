# fp63 screen: the base share of the high's hour fails (2026-09-25)

The protocol was committed before this run (`51fa8e1`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp63/screen_2023.json`, sha256
`afe502385695c998473e730382d16f81e5f21b19e3a1aaf8b1be323011c2a63a`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| HIGH-BASE | 26 | +70.54 bps | +81.22 bps | +$18.34 | no |

The count is under 30 and stays there. The file's null is +81.2211 bps,
the same figure as §3.47, because both rules have 26 trades on the same
BTC pool and the same seed. The mean is positive and under the null. The
gross move is +90.70 bps; the 10 bps-a-side fill leaves +70.54. Every fill
matches fp5's `net_return` on the next BTC open. All 26 signal days have a
trade. The hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC.
That day is not a print. It was not filled in. 456 days have a share. The
daily grid has 458 bars and no hole. A smaller base share in the high's
hour, and the 80th, are not scored. The rule is not fit to add.
