# fp92 screen: the opening hour's quote over the last hour's fails (2026-09-25)

The protocol was committed before this run (`9614bb3`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp92/screen_2023.json`, sha256
`a13e47248431b47a47f1ba1834b93954b9da9ecd4880b5e443e4b5e6651cc026`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| ENDS | 39 | −8.59 bps | +66.26 bps | −$3.35 | no |

The count cleared. The file's gross is +11.4092 bps and the file's mean is
−8.5936 bps. The file's null is +66.261 bps, the same figure as §3.117,
because both rules have 39 trades on the same BTC pool and the same seed.
The mean is negative and under the null. The total is −$3.3515. Every fill
matches fp5's `net_return` on the next BTC open. All 39 signal days have a
trade. The hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC.
Hours 1 through 22 are not read, so that day is still a print. The hole was
not filled in. 457 days have a ratio. The daily grid has 458 bars and no
hole. An opening hour that was quieter than the last hour, and the 80th,
are not scored. The rule is not fit to add.
