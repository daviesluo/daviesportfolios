# fp87 screen: the close above the day's VWAP fails (2026-09-25)

The protocol was committed before this run (`9614bb3`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp87/screen_2023.json`, sha256
`8a2be7dcc71bc91ccf3feb86305c6551e930faa09678fd0d6ea52120d0192bb6`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| STRETCH | 43 | +54.69 bps | +66.23 bps | +$23.52 | no |

The count cleared. The file's gross is +74.8196 bps and the file's mean is
+54.6901 bps. The file's null is +66.2261 bps, the same figure as §3.62,
because both rules have 43 trades on the same BTC pool and the same seed.
The mean is positive and under the null. The total is +$23.5167. Every fill
matches fp5's `net_return` on the next BTC open. All 43 signal days have a
trade. The daily grid has 458 bars and no hole. The 2024-01-01 bar is not a
print, so 457 days have a stretch. A close that finished nearer the VWAP,
and the 80th, are not scored. The rule is not fit to add.
