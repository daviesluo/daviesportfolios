# fp33 screen: today's low over yesterday's fails (2026-09-25)

The protocol was committed before this run (`1f9c4e5`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp33/screen_2023.json`, sha256
`83a3ff7228852a01b35837c16fb207d20aaffc84995f7ab9bb5d3a4ac4e8ef01`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| LOW-LIFT | 43 | +45.49 bps | +66.23 bps | +$19.56 | no |

The count clears and the mean is positive. It loses to the null. The gross
move is +65.60 bps; the 10 bps-a-side fill leaves +45.49. Every fill matches
fp5's `net_return` on the next day's open. All 43 signal days have a trade.
The daily grid has 458 bars and no hole. The first day has no prior low, so
there are 456 lifts. The 2024-01-01 bar is an exit print, with its low set
equal to the open. A lower low is not scored. The 80th is not scored. The
rule is not fit to add.
