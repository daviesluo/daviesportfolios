# fp49 screen: hours that closed above the prior hour fails (2026-09-25)

The protocol was committed before this run (`169d5ce`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp49/screen_2023.json`, sha256
`1d7aed02bfb131710cca743b8b131523383fb66796e33f05057bbf278cac5379`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| UP-STEP | 25 | +127.76 bps | +87.12 bps | +$31.94 | no |

The count is under 30 and stays there. The file's mean net is +127.755 bps.
The mean is positive and above the null. That does not clear the screen:
the count of 30 is not lowered, and the later years are not pulled. The
gross move is +148.03 bps; the 10 bps-a-side fill leaves +127.76. Every
fill matches fp5's `net_return` on
the next BTC open. All 25 signal days have a trade. The hourly grid has
10,967 bars and one hole, 2023-03-24 13:00 UTC, so that day is not a print.
It was not filled in. The daily grid has 458 bars and no hole. 456 days have
a share. A day with fewer rising hourly closes, and the 80th, are not
scored. The rule is not fit to add.
