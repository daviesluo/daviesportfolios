# fp94 screen: the last close over the first hour's close fails (2026-09-25)

The protocol was committed before this run (`9614bb3`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp94/screen_2023.json`, sha256
`88120c0911e9121b8ac1a18683342088a7fcae15ba3ad55c2ae7a5c6a81fd449`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| REST | 47 | +13.65 bps | +66.66 bps | +$6.42 | no |

The count cleared. The file's gross is +33.6986 bps and the file's mean is
+13.6513 bps. The file's null is +66.6563 bps. No earlier screen had 47
trades, so the null has no earlier file to match. It is fp5's 200-draw p95
on the same BTC pool and the same seed. The mean is positive and under the
null. The total is +$6.4161. Every fill matches fp5's `net_return` on the
next BTC open. All 47 signal days have a trade. The hourly grid has 10,967
bars and one hole, 2023-03-24 13:00 UTC. Hours 1 through 22 are not read, so
that day is still a print. The hole was not filled in. 457 days have a
return. The daily grid has 458 bars and no hole. A last close under the
first hour's close, and the 80th, are not scored. The rule is not fit to
add.
