# fp48 screen: the rise in average trade size passes 2023 (2026-09-25)

The protocol was committed before this run (`169d5ce`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp48/screen_2023.json`, sha256
`293190d9d8ceed81ac2bc3862b3b3ef09bc0ec551c78ee7596e43940204e8c55`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| AVG-CHG | 48 | +77.64 bps | +60.96 bps | +$37.27 | yes, the screen only |

The count clears. The mean is above the null. The file's mean net is
+77.6367 bps and the total is +$37.2656. The null p95 is +60.9592 bps, the
same figure as the prior-close-high screen, because both rules have 48
trades on the same BTC daily pool and the same seed. The gross move is
+97.81 bps; the 10 bps-a-side fill leaves +77.64. Every fill matches fp5's
`net_return` on the next BTC open. All 48 signal days have a trade. The
daily grid has 458 bars and no hole. The first day has no prior size, so
there are 456 changes. The 2024-01-01 quote and trade count are stored as 0.
A day the average size did not rise stays in the history and is not a
signal. A 2023 pass is not adoption. The later years are in
`2026-09-25-fp48-size-result.md`.
