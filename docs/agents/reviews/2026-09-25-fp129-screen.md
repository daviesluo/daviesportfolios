# fp129 screen: average block size fails (2026-09-25)

The protocol was committed before this run (`a0e6d12`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp129/screen_2023.json`, sha256
`6b291229c55af00b2babac0d0112b82a584bb1d98eaf496e0c5df58bb6db71f3`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| BLKSZ | 47 | +50.3544 bps | +66.6563 bps | +$23.6665 | no |

The file's gross is +70.4752 bps and the file's mean is +50.3544 bps.
The file's null is +66.6563 bps. The total is +$23.6665. The null is BTC's
own one-day holds. The mean is positive and under the null. The window stays 90 days
and 90 prints. Every fill matches fp5's `net_return`. 457 prints, from
2022-10-01 through 2023-12-31, with no missing day. No point in 2024 was
stored. 47 signal days, 47 trades. Hash rate, difficulty, and the block count were not read. The rule is not fit to add.
