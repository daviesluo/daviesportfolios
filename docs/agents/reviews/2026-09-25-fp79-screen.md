# fp79 screen: the busiest hour's return fails (2026-09-25)

The protocol was committed before this run (`409b715`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp79/screen_2023.json`, sha256
`97d363a1f129a604c27e5705c030bf68f242ebd5a6ca7f86cf8bad5f0c766e82`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| BUSY-RET | 35 | +14.20 bps | +78.43 bps | +$4.97 | no |

The count clears. The file's gross is +34.2437 bps and the file's mean is
+14.1953 bps. The file's null is +78.4258 bps, the same figure as §3.75,
because both rules have 35 trades on the same BTC pool and the same seed.
The mean is positive and under the null. The total is +$4.9684. Every fill
matches fp5's `net_return` on the next BTC open. All 35 signal days have a
trade. The hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC.
That day is not a print. It was not filled in. 456 days have a return. The
daily grid has 458 bars and no hole. A quieter busiest hour, and the 80th,
are not scored. The rule is not fit to add.
