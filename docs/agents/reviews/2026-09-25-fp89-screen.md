# fp89 screen: the worst hourly return fails (2026-09-25)

The protocol was committed before this run (`9614bb3`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp89/screen_2023.json`, sha256
`5809aefa28648a91fa9ae5524f3d65cd86f86044e08afd24ada54a0582845401`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| WORST-RET | 23 | −16.30 bps | +85.93 bps | −$3.75 | no |

The count is under 30 and stays there. The file's gross is +3.6829 bps and
the file's mean is −16.3045 bps. The file's null is +85.9259 bps, the same
figure as §3.52, because both rules have 23 trades on the same BTC pool and
the same seed. The mean is negative and under the null. The total is
−$3.75. Every fill matches fp5's `net_return` on the next BTC open. All 23
signal days have a trade. The hourly grid has 10,967 bars and one hole,
2023-03-24 13:00 UTC. That day is not a print. It was not filled in. 456
days have a worst return. The daily grid has 458 bars and no hole. A weaker
worst hour, and the 80th, are not scored. The rule is not fit to add.
