# fp121 screen: put contracts over call contracts — no fill (2026-09-25)

The protocol was committed before this run (`502b701`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp121/screen_2023.json`, sha256
`3004433958542b6a7e4f3f2d9cecd62a6750627e242865f83e824356c6dc5cfb`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| PCOI | 0 | — | — | $0 | no |

This run does not count. A scored rule with no fill is not one of the
eight. The file's trade count is 0, so the file has no mean and no null.
The total is 0. 147 prints, from 2023-05-18 through 2023-10-23. Zero of
those prints were strictly above their own trailing 90th on a day that
already had 90 earlier prints inside 90 days. The September holes were
not filled in, and the window was not widened. The reciprocal was not
scored. Put volume over call volume is a different rule and was scored
on its own. The rule is not fit to add.
