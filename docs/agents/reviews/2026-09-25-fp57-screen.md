# fp57 screen: the rise in base volume passes 2023 (2026-09-25)

The protocol was committed before this run (`68b331f`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp57/screen_2023.json`, sha256
`faa314200e5b468c0b3b10886fdc527a8d1cea63e486939428accd6e12250cd0`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| BASE-CHG | 44 | +65.20 bps | +63.98 bps | +$28.69 | yes, the screen only |

The count clears. The mean is above the null. The file's mean net is
+65.1976 bps and the total is +$28.6869. The null p95 is +63.9818 bps. The
gross move is +85.35 bps; the 10 bps-a-side fill leaves +65.20. Every fill
matches fp5's `net_return` on the next BTC open. All 44 signal days have a
trade. The daily grid has 458 bars and no hole. The first day has no prior
base volume, so there are 456 changes. The 2024-01-01 base volume is stored
as 0. A day base volume did not rise stays in the history and is not a
signal. A 2023 pass is not adoption. The later years are in
`2026-09-25-fp57-base-result.md`.
