# fp43 screen: quote volume in up hours fails (2026-09-25)

The protocol was committed before this run (`ff4d925`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp43/screen_2023.json`, sha256
`832ecb42030ed2d32235ceee2f653d5feddc028bd8278069fed9e58ef89c7a70`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| UP-VOL | 33 | +53.99 bps | +74.79 bps | +$17.82 | no |

The count clears and the mean is positive. It loses to the null. The null
is the same figure as STREAK's, because both rules have 33 trades on the
same BTC daily pool and the same seed. The gross move is +74.12 bps; the
10 bps-a-side fill leaves +53.99. Every fill matches fp5's `net_return` on
the next BTC open. All 33 signal days have a trade. The hourly grid has
10,967 bars and one hole, 2023-03-24 13:00 UTC, so that day is not a print.
It was not filled in. The daily grid has 458 bars and no hole. 456 days
have a share. The down-volume side, and the 80th, are not scored. The rule
is not fit to add.
