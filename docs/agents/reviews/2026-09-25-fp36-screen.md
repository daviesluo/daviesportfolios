# fp36 screen: the open inside yesterday's range fails (2026-09-25)

The protocol was committed before this run (`1f9c4e5`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp36/screen_2023.json`, sha256
`42f1ce9829c0a230a70a33a1182b5e1438ec9b9b2982d3e82956359a1d1cfb1a`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| OPEN-LOC | 42 | +0.03 bps | +67.42 bps | +$0.01 | no |

The count clears. The mean is positive by +0.0326 bps, which is +$0.01 on
the $100, and it loses to the null. The gross move is +20.05 bps; the 10
bps-a-side fill leaves +0.03. Every fill matches fp5's `net_return` on the
next day's open, and that open is not the open in the signal. All 42 signal
days have a trade. The daily grid has 458 bars and no hole. The first day
has no prior range, so there are 456 locations. The 2024-01-01 high and low
are stored equal to the open. An open low in yesterday's range is not
scored. The 80th is not scored. The rule is not fit to add.
