# fp80 screen: the VWAP's place in the range fails (2026-09-25)

The protocol was committed before this run (`409b715`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp80/screen_2023.json`, sha256
`32bced08150fe2693681beb8499f724131202a29c0a9932c6265fdfd8d42a54e`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| VWAP-LOC | 40 | −30.43 bps | +67.59 bps | −$12.17 | no |

The count clears. The file's gross is −10.4695 bps and the file's mean is
−30.4286 bps. The file's null is +67.5914 bps, the same figure as §3.84,
because both rules have 40 trades on the same BTC pool and the same seed.
The mean is negative and under the null. The total is −$12.1714. Every fill
matches fp5's `net_return` on the next BTC open. All 40 signal days have a
trade. The daily grid has 458 bars and no hole. The masked horizon is not a
print, so the window has 457 locations. The 2024-01-01 high and low are
stored equal to that bar's open, and its quote and base volume are stored
as zero. That bar is an exit print only. A VWAP lower in the range, and the
80th, are not scored. The rule is not fit to add.
