# fp120 screen: option contract volume fails (2026-09-25)

The protocol was committed before this run (`502b701`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp120/screen_2023.json`, sha256
`b1cccc6bea044ff7855d5aee74d4aed9638b12b12d67761670ad7885a20ea0a5`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| OPTVOL | 7 | −94.1282 bps | +145.7716 bps | −$6.589 | no |

The count did not clear. The file's gross is −74.2966 bps and the file's
mean is −94.1282 bps. The file's null is +145.7716 bps. The total is
−$6.589. The null is BTC's own one-day holds. The mean is negative, and
7 trades are under 30. The count stays 30. The window stays 90 days and
90 prints. Every fill matches fp5's `net_return`. 147 prints, from
2023-05-18 through 2023-10-23. The missing September days were not filled
in. The snapshot is the last hour's `volume_contracts`, not a sum of the
earlier hours and not `volume_usdt`. 7 signal days, 7 trades. Premium open
interest is a different rule. The rule is not fit to add.
