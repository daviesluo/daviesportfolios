# fp46 screen: the last close versus the day's average fails (2026-09-25)

The protocol was committed before this run (`169d5ce`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp46/screen_2023.json`, sha256
`d6feef83bccc056efe287be7de92b3f9084be284c8668cec17b85e99433b5628`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| CLOSE-MEAN | 35 | +37.50 bps | +78.43 bps | +$13.12 | no |

The count clears and the mean is positive. It loses to the null. The gross
move is +57.59 bps; the 10 bps-a-side fill leaves +37.50. Every fill matches
fp5's `net_return` on the next BTC open. All 35 signal days have a trade.
The hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC, so that
day is not a print. It was not filled in. The daily grid has 458 bars and
no hole. 456 days have a distance. A close under the day's average, and the
80th, are not scored. The rule is not fit to add.
