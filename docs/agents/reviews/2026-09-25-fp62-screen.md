# fp62 screen: the low under yesterday's close fails (2026-09-25)

The protocol was committed before this run (`51fa8e1`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp62/screen_2023.json`, sha256
`a9b659dae3c3ba3e607dea8288ffbf83780c8d9279bd677ffb2147e62e9d82b8`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| UNDER | 42 | +56.20 bps | +67.42 bps | +$23.60 | no |

The count clears. The file's null is +67.4226 bps, the same figure as
§3.65, because both rules have 42 trades on the same BTC pool and the same
seed. The mean is positive and under the null. The gross move is +76.33
bps; the 10 bps-a-side fill leaves +56.20. Every fill matches fp5's
`net_return` on the next BTC open. All 42 signal days have a trade. The
daily grid has 458 bars and no hole. The first day has no prior close, so
there are 456 distances. The 2024-01-01 low and close are stored equal to
that bar's open, which is an exit print only. A shallower undercut, and the
80th, are not scored. The rule is not fit to add.
