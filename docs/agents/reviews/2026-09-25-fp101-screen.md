# fp101 screen: LINK quote over BTC quote fails (2026-09-25)

The protocol was committed before this run (`6b35a2c`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp101/screen_2023.json`, sha256
`eb49f3a10377269577b8f4ae6ece8d3ec8b723e1a5a08dcc6dc2451872115376`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| LINK-Q | 82 | +24.06 bps | +76.738 bps | +$19.73 | no |

The count cleared. The file's gross is +44.1264 bps and the file's mean is
+24.0582 bps. The file's null is +76.738 bps. The total is +$19.7277. The
null is LINK's own one-day holds, so it does not match a BTC screen. The mean
is positive and under the null. Every fill matches fp5's `net_return` on the
next LINK open. All 82 signal days have a trade. The daily grid has 458 bars.
The 2024-01-01 quote is stored as zero and is not a print, so 457 days have a
ratio. No hourly bar was read. ETH was not scored. A day LINK was quiet versus
BTC, and the 80th, are not scored. The rule is not fit to add.
