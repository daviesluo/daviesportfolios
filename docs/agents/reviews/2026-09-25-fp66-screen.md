# fp66 screen: the hour after the high has no fill (2026-09-25)

The protocol was committed before this run (`51fa8e1`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp66/screen_2023.json`, sha256
`c8455493b2e4dcce4ac59c84dc67078c0501ce558d95bd1539f63f52a5036b35`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| AFTER-HIGH | 0 | | | $0 | no |

This scored run has no fill, so it does not count toward the eight. The
hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC. That day
is not a print. It was not filled in. 456 days have all 24 hours. 24 of
those days print the high in hour 23, and the frozen rule drops them, so
there are 432 prints and no signal. The longest trailing window among those
prints holds 89 earlier prints. The rank asks for 90, so no threshold
exists. The 90-print window is not widened, and the hour-23 skip is not
dropped. The daily grid has 458 bars and no hole. The rule is not fit to
add.
