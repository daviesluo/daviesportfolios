# fp22 screen: volume concentration fails (2026-09-25)

The protocol was committed before this run (`3426f0c`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp22/screen_2023.json`, sha256
`ebf5ec3bbaa367662a47165952c598a5050310ac5e326bb2ad300f280d880a6e`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| VOL-HHI | 29 | +68.57 bps | +79.18 bps | +$19.89 | no |

The count is under 30, and it stays 30. The mean is positive and still
below the null. The gross move is +88.73 bps; the 10 bps-a-side fill
leaves +68.57. Every fill matches fp5's `net_return` on the next day's
open. The daily grid has 458 bars and no hole. One hour is missing,
2023-03-24 13:00 UTC, so that day is not a print. Filling it in would not
be this screen. The 2024-01-01 bar is an exit print and not a signal. An
even day is not scored. The 80th is not scored. The rule is not fit to add.
