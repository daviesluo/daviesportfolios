# fp75 protocol: crosses of today's open (2026-09-25)

Written before any return of this rule was computed. The rules in fp5
through fp70 are dead, including LS-FADE, BODY and its out-of-sample
result, BAL-CHG, IMPACT and its out-of-sample result, PEAK, STREAK,
OPEN-DRIVE, AVG-CHG and its out-of-sample result, the screens fp30
through fp61, and fp62 through fp70. VOL-CLOCK and BASE-CHG failed out
of sample. Both of their neighbours lost, and neither neighbour is a
candidate. UNDER, HIGH-BASE, MID-RET, ETH-SPAN, CHOP, CROSS, EXPAND and
PRIOR-LOC failed the 2023 screen. AFTER-HIGH was scored and produced no
fill. It does not count, and the 90-print window is not widened to make
it fire. None of their cuts is reused, and no sign is flipped. IMPACT's
80th and 95th are not candidates. BODY's 80th is not a candidate. PEAK's
close under the prior 30-day high, and its 80th, are not candidates.
STREAK's, OPEN-DRIVE's and AVG-CHG's 80th and 95th are not candidates.
AVG-CHG's 95th was positive and does not save it. The level of the
address count is not a candidate, and neither is BAL-CHG's 80th.
UP-STEP beat its null on 25 trades. The count stays 30, and those later
years were not pulled. CLOSE-HIGHS had 28 trades and LAST-EXT had 19.
HIGH-BASE had 26, CROSS had 24 and EXPAND had 27. The count stays 30.
A rule rejected without a scored run does not count. The count of 30 is
not lowered. The window is not shrunk.

This screen is the only measurement this file allows. A pass is not an
adoption. It earns a pre-registration before any later year is scored. 2023
is spent once this screen runs.

Nothing here changes a frozen spec, arms a row, or sends an order. The fill
is fp5's `net_return`. Public reads only. No hourly bar from 2024 onward is stored. Hourly bars stop before the 2024-01-01 open. That daily open may be an exit print. A later open is a bug.

## Not this search

* Crosses of yesterday's 23:00 close. CROSS stays closed. Yesterday is not read.
* Sign changes of the hourly return. TURN stays closed. A sign change that stays on one side of today's open is not a cross.
* Today's open divided by yesterday's close. GAP-UP stays closed.
* A close equal to the open is not a side and does not reset the previous side. The walk starts on the open, so the first departure is not a cross.

## Costs, window, null

Ten basis points a side. No spread on this screen. Entries in
`[2023-01-01, 2024-01-01)` UTC. History for a threshold may start on
2022-10-01. The null is fp5's: 200 draws, seed `20250925`, index 190,
drawn from every in-screen BTC daily hold. A pass is at least 30 trades,
mean net above zero, and mean net above the null.

## The idea

**OPEN-X.** BTCUSDT. On day D, strictly before 2024-01-01, count how many of the day's 24 hourly closes cross the day's own open, which is hour 0's open. All 24 hours must exist. Yesterday's close is not an input. The statistic is known when D closes, and its timestamp is D.

It fires when the statistic is strictly above its own trailing-90-day 90th
percentile, at least 90 earlier prints in `[D−90 days, D)`, percentile
`sorted[floor(0.90 × (n − 1))]`. Enter the next BTCUSDT daily open. Hold
one day. Fewer crosses of today's open are not a separate rule.

The 80th and the 95th percentiles of the same statistic are named as
neighbours. They are a veto that can only reject, and only after a
pre-registration. They are not scored on this screen.

## What a pass becomes

The house bar used for LS-FADE: both later sub-windows positive, above a
fresh null, positive with doubled costs, at least 30 trades, no month above
40% of the profit and the rest positive, and more than 4% a year on the $100
it locks, entries 2024-01-01 through 2026-09-24. 2023 is not part of that
bar. If this rule fails, it is dead and the next search does not inherit
the window or the cut. The 80th and the 95th of this statistic are a veto
that can only reject, and only after a pre-registration. They are not scored
on this screen.
