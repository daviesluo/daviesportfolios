# fp116 screen: spot trade count over perpetual trade count fails (2026-09-25)

The protocol was committed before this run (`e291a05`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp116/screen_2023.json`, sha256
`1487fb73812ceba08995bcb78ceb11e40615103fc7a546a24b414f9b116a05da`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| TRDMIX | 61 | −24.0184 bps | +53.0762 bps | −$14.6512 | no |

The count cleared. The file's gross is −4.0465 bps and the file's mean is
−24.0184 bps. The file's null is +53.0762 bps. The total is −$14.6512. The
null is BTC's own one-day holds. It matches OITURN's null because both
screens have 61 trades on the same BTC opens. The mean is negative. Every
fill matches fp5's `net_return`. 457 prints. 62 days cleared the 90th. The
2023-12-31 print would enter on 2024-01-01, which is outside the screen, so
the trades are 61. Quote volume is not stored. The 2024-01-01 trade count
is stored as 0. The perpetual series stops on 2023-12-31. No funding print
was read. No hourly price bar was read. No alt quote was read. The
trade-count level is not the statistic. The reciprocal is not scored. A day
below the 90th is not scored. The rule is not fit to add.
