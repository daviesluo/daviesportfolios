# fp19 screen: close location fails (2026-09-25)

The protocol was committed before this run (`b14c3c5`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp19/screen_2023.json`, sha256
`b87d4bd70cf44f14d3e907d90fc3c92aa42a5c103400b1e04760fc2be6cd5f59`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| CLOSE-LOC | 41 | +12.06 bps | +70.16 bps | +$4.95 | no |

The count clears 30, and the mean is positive. It does not beat the null.
The gross move is +32.11 bps; the 10 bps-a-side fill leaves +12.06. Every
fill matches fp5's `net_return` on that day's open. The daily grid has 458
bars and no hole. The 2024-01-01 bar is an exit print and not a signal. The
low location is not scored. The 80th is not scored. The rule is not fit to add.
