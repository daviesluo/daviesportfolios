# fp88 screen: the low above yesterday's high fails (2026-09-25)

The protocol was committed before this run (`9614bb3`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp88/screen_2023.json`, sha256
`0d16e52549a585c3a6aa8676e85a6dea21b42fba87ff2040218cfeefc87f5632`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| HOLD | 39 | +14.96 bps | +66.26 bps | +$5.83 | no |

The count cleared. The file's gross is +35.0109 bps and the file's mean is
+14.9609 bps. The file's null is +66.261 bps. No earlier screen had 39
trades. ENDS, scored in the same round, prints the same null. The mean is
positive and under the null. The total is +$5.8347. Every fill matches
fp5's `net_return` on the next BTC open. All 39 signal days have a trade.
The daily grid has 458 bars and no hole. The first day has no yesterday,
and the 2024-01-01 bar is not a print, so 456 days have a hold. A low that
did not hold above yesterday's high, and the 80th, are not scored. The rule
is not fit to add.
