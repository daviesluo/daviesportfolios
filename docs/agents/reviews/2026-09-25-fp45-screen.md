# fp45 screen: the close versus yesterday's high fails (2026-09-25)

The protocol was committed before this run (`ff4d925`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp45/screen_2023.json`, sha256
`b104a4b2d7ed2da75be8a8a0d2bd6934f16448b93df92af64d59260afc89d7dd`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| YDAY-HIGH | 43 | +44.73 bps | +66.23 bps | +$19.23 | no |

The count clears and the mean is positive. It loses to the null. The null
is the same figure as LOW-LIFT's, because both rules have 43 trades on the
same BTC daily pool and the same seed. The gross move is +64.84 bps; the
10 bps-a-side fill leaves +44.73. Every fill matches fp5's `net_return` on
the next BTC open. All 43 signal days have a trade. The daily grid has 458
bars and no hole. The first day has no prior high, so there are 456
distances. The 2024-01-01 high and close are stored equal to the open. A
close under yesterday's high, and the 80th, are not scored. This is not the
prior 30-day close high, and that rule stays dead. The rule is not fit to
add.
