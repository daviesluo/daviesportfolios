# fp35 screen: the open versus yesterday's VWAP fails (2026-09-25)

The protocol was committed before this run (`1f9c4e5`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp35/screen_2023.json`, sha256
`9acfacc952c3cff3eda55d11d40bf4839aaff0dba4ecdf6c145e46e96d579fca`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| OPEN-VWAP | 44 | +42.06 bps | +63.98 bps | +$18.50 | no |

The count clears and the mean is positive. It loses to the null. The gross
move is +62.16 bps; the 10 bps-a-side fill leaves +42.06. Every fill matches
fp5's `net_return` on the next day's open, and that open is not the open in
the signal. All 44 signal days have a trade. The daily grid has 458 bars and
no hole. The first day has no prior VWAP, so there are 456 distances. The
2024-01-01 bar is an exit print, with base and quote volume stored as 0. An
open under yesterday's VWAP is not scored. The 80th is not scored. The rule
is not fit to add.
