# fp83 screen: the close of the high's hour fails (2026-09-25)

The protocol was committed before this run (`409b715`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp83/screen_2023.json`, sha256
`e2edf4dab62b0ab56a25940688f2d11ab7377612b6727604c040c533638190c4`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| HIGH-CLOSE | 37 | +31.05 bps | +74.56 bps | +$11.49 | no |

The count clears. The file's gross is +51.1289 bps and the file's mean is
+31.0467 bps. The file's null is +74.5602 bps, the same figure as §3.94,
because both rules have 37 trades on the same BTC pool and the same seed.
The mean is positive and under the null. The total is +$11.4873. Every fill
matches fp5's `net_return` on the next BTC open. All 37 signal days have a
trade. The hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC.
That day is not a print. It was not filled in. 456 days have a distance. The
daily grid has 458 bars and no hole. A high hour that closed nearer the
open, and the 80th, are not scored. The rule is not fit to add.
