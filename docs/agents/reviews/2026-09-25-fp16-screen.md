# fp16 screen: intraday autocorrelation fails (2026-09-25)

The protocol was committed before this run (`3d07eca`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp16/screen_2023.json`, sha256
`31086946ec2d96e4f5bf60d0cb92802be2397c93194a92833900d9abef41b285`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| AC-HOUR | 27 | +23.76 bps | +81.29 bps | +$6.41 | no |

The count is under 30, and it stays there. The gross move is +43.82 bps.
After the 10 bps-a-side fill the mean is still positive, and it is below
the null. Every fill matches fp5's `net_return` on that day's open. One
hourly bar is missing, 2023-03-24 13:00 UTC, so that day is not a print.
Filling it would not reach 30 trades, and the mean already loses to the
null. The negative tail is not scored. The 80th is not scored. The rule is
not fit to add.
