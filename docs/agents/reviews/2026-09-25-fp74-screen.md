# fp74 screen: the return from hour 0 to hour 6 fails (2026-09-25)

The protocol was committed before this run (`6c35982`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp74/screen_2023.json`, sha256
`951f749d1e51ffebcd7febaef8da946c6ffb40994b5768c3278b74574db67b4c`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| MORNING | 36 | +57.98 bps | +72.62 bps | +$20.87 | no |

The count clears. The file's mean is +57.984 bps and the file's null is
+72.6224 bps, the same null as §3.73, because both rules have 36 trades on
the same BTC pool and the same seed. The mean is positive and under the
null. The gross move is +78.12 bps. Every fill matches fp5's `net_return`
on the next BTC open. All 36 signal days have a trade. The hourly grid has
10,967 bars and one hole, 2023-03-24 13:00 UTC. That hole is after hour 6,
so the day stays a print. It was not filled in. 457 days have a morning
return. The daily grid has 458 bars and no hole. A weak run from hour 0 to
hour 6, and the 80th, are not scored. The rule is not fit to add.
