# fp130 screen: median confirmation time fails (2026-09-25)

The protocol was committed before this run (`a0e6d12`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp130/screen_2023.json`, sha256
`659c05ad6b71cf79ffedd74f2290d119373bf24c401e3cc6aab0a547f9bbb6db`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| MEDCONF | 48 | +6.6079 bps | +60.9592 bps | +$3.1718 | no |

The file's gross is +26.6411 bps and the file's mean is +6.6079 bps.
The file's null is +60.9592 bps. The total is +$3.1718. The null is BTC's
own one-day holds. The mean is positive and under the null. The window stays 90 days
and 90 prints. Every fill matches fp5's `net_return`. 457 prints, from
2022-10-01 through 2023-12-31, with no missing day. No point in 2024 was
stored. 48 signal days, 48 trades. The average confirmation time was not read. Mempool size was not read. The rule is not fit to add.
