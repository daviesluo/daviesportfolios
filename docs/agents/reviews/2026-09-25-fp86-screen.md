# fp86 screen: the overlap of consecutive hours fails (2026-09-25)

The protocol was committed before this run (`409b715`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp86/screen_2023.json`, sha256
`d5810bcbff2aeeb854279e7391fc45466941e37d721b691a623db85d38cfac05`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| OVERLAP | 23 | −13.18 bps | +85.93 bps | −$3.03 | no |

The count is under 30 and stays there. The file's gross is +6.8138 bps and
the file's mean is −13.1798 bps. The file's null is +85.9259 bps, the same
figure as §3.52, because both rules have 23 trades on the same BTC pool and
the same seed. The mean is negative and under the null. The total is
−$3.0314. Every fill matches fp5's `net_return` on the next BTC open. All
23 signal days have a trade. The hourly grid has 10,967 bars and one hole,
2023-03-24 13:00 UTC. That day is not a print. It was not filled in. 456
days have an overlap. The daily grid has 458 bars and no hole. A day whose
hours overlapped less, and the 80th, are not scored. The rule is not fit to
add.
