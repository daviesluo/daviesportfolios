# fp99 screen: ADA quote over BTC quote fails (2026-09-25)

The protocol was committed before this run (`6b35a2c`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp99/screen_2023.json`, sha256
`6ed6b01e5a0d4662f6aae4d35c165f84b49c924ed90e30ce2e138155cff7bfe1`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| ADA-Q | 71 | −5.83 bps | +85.30 bps | −$4.14 | no |

The count cleared. The file's gross is +14.1812 bps and the file's mean is
−5.8271 bps. The file's null is +85.2975 bps. The total is −$4.1373. The
null is ADA's own one-day holds, so it does not match a BTC screen. The mean
is negative. Every fill matches fp5's `net_return` on the next ADA open. All
71 signal days have a trade. The daily grid has 458 bars. The 2024-01-01
quote is stored as zero and is not a print, so 457 days have a ratio. No
hourly bar was read. ETH was not scored. A day ADA was quiet versus BTC, and
the 80th, are not scored. The sign is not flipped. The rule is not fit to add.
