# fp21 screen: close versus VWAP fails (2026-09-25)

The protocol was committed before this run (`16c34db`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp21/screen_2023.json`, sha256
`99c2dee182bb1420e025ea761ffbf72f49c1f309de6e7ec387155e2c24c8624e`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| VWAP-PREM | 44 | +28.10 bps | +63.98 bps | +$12.36 | no |

The count clears 30, and the mean is positive. It does not beat the null.
The gross move is +48.17 bps; the 10 bps-a-side fill leaves +28.10. Every
fill matches fp5's `net_return` on the next day's open. The daily grid has
458 bars and no hole. Thirty-one of the 44 signal days opened at or below
the previous close, so the premium is not the overnight gap. The 2024-01-01
bar is an exit print and not a signal. A close under the VWAP is not
scored. The 80th is not scored. The rule is not fit to add.
