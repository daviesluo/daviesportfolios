# fp75 screen: crosses of today's open fail (2026-09-25)

The protocol was committed before this run (`6c35982`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp75/screen_2023.json`, sha256
`c1111ff5648efa037942f9ada6fcab7c5d36d9630239960b74ce42cb5916d288`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| OPEN-X | 24 | +25.41 bps | +86.81 bps | +$6.10 | no |

The count is under 30 and stays there. The file's gross is +45.478 bps,
the file's null is +86.8142 bps, and the total is +$6.0977. The 24 entry
days are the same days as §3.97, so the fill record matches that screen.
The level here is today's open. Yesterday's close is not read. The null
matches §3.97 because both rules have 24 trades on the same BTC pool and
the same seed. The mean is positive and under the null. Every fill matches
fp5's `net_return` on the next BTC open. All 24 signal days have a trade.
The hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC. That
day is not a print. It was not filled in. 456 days have a count. The daily
grid has 458 bars and no hole. Fewer crosses, and the 80th, are not scored.
The rule is not fit to add.
