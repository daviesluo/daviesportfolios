# fp29 screen: distance from the prior close high fails (2026-09-25)

The protocol was committed before this run (`d235b84`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp29/screen_2023.json`, sha256
`3f7c62a0f7872078953f91004e60aad4223cdeab9d75266ada137b74fc81b141`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| PEAK | 48 | +18.22 bps | +60.96 bps | +$8.75 | no |

The count clears and the mean is positive. It loses to the null. The gross
move is +38.28 bps; the 10 bps-a-side fill leaves +18.22. Every fill matches
fp5's `net_return` on the next day's open. All 48 signal days have a trade.
The daily grid has 458 bars and no hole. The first 30 days have no prior
window, so there are 427 distances. The 2024-01-01 bar is an exit print,
with its close set equal to the open. A close under the prior 30-day high
is not scored. The 80th is not scored. The rule is not fit to add.
