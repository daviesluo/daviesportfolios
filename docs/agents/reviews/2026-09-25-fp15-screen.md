# fp15 screen: the realized-vol ratio fails (2026-09-25)

The protocol was committed before this run (`2ba98e4`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp15/screen_2023.json`, sha256
`af54b443d5c062f557d9eb4a9b7edddb63d39069d9f1e37db1167ece08c0ecd4`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| RV-RATIO | 31 | −19.88 bps | +71.83 bps | −$6.16 | no |

The count clears 30. The gross move is +0.10 bps, and the 10 bps-a-side fill
takes the mean to −19.88 bps. It does not beat the null. Every fill matches
fp5's `net_return` on that bar's open. The low ratio is not scored. The
quiet-day range and the implied-minus-realized gap are not reopened. The
rule is not fit to add.
