# fp64 screen: the return from hour 8 to hour 15 fails (2026-09-25)

The protocol was committed before this run (`51fa8e1`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp64/screen_2023.json`, sha256
`91f6a430c7d42dcb4eb89395be71dc787fef5cc5f0e74496240283da367c738d`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| MID-RET | 41 | −0.77 bps | +70.16 bps | −$0.32 | no |

The count clears. The file's mean is −0.7749 bps and the file's null is
+70.1636 bps, the same null as §3.89, because both rules have 41 trades on
the same BTC pool and the same seed. The mean is negative and under the
null. The gross move is +19.24 bps; the 10 bps-a-side fill leaves −0.77.
Every fill matches fp5's `net_return` on the next BTC open. All 41 signal
days have a trade. The hourly grid has 10,967 bars and one hole, 2023-03-24
13:00 UTC. That hole sits inside hours 8 through 15, so the day is not a
print. It was not filled in. 456 days have a middle return. The daily grid
has 458 bars and no hole. A weak middle block, and the 80th, are not
scored. The rule is not fit to add.
