# fp39 screen: hours sitting inside the previous hour fails (2026-09-25)

The protocol was committed before this run (`ff4d925`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp39/screen_2023.json`, sha256
`c8ede5943dff2f72630181e606c7b3dc74f0d98bb8bf5e617c8ee23127e65342`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| INSIDE | 10 | −56.14 bps | +117.12 bps | −$5.61 | no |

The count is under 30 and stays there. The mean is negative and under the
null. The gross move is −36.24 bps; the 10 bps-a-side fill leaves −56.14.
Every fill matches fp5's `net_return` on the next BTC open. There are 11
signal days. Ten have a trade. The 2023-12-31 signal would exit on
2024-01-02, which is outside the screen, so it is not a 2023 fill. The
hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC, so that day
is not a print. It was not filled in. The daily grid has 458 bars and no
hole. 456 days have a count. A day with fewer inside hours, and the 80th,
are not scored. The rule is not fit to add.
