# fp52 screen: the rise in quote volume fails (2026-09-25)

The protocol was committed before this run (`169d5ce`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp52/screen_2023.json`, sha256
`e9cfb2dc6b5378bc9a001fd207eba142519e832ee6092f23c73327743560f37b`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| QVOL-CHG | 45 | +61.80 bps | +64.27 bps | +$27.81 | no |

The count clears and the mean is positive. It loses to the null. The gross
move is +81.95 bps; the 10 bps-a-side fill leaves +61.80. Every fill matches
fp5's `net_return` on the next BTC open. All 45 signal days have a trade.
The daily grid has 458 bars and no hole. The first day has no prior quote,
so there are 456 changes. The 2024-01-01 quote is stored as 0. A day quote
volume did not rise stays in the history and is not a signal. The 80th is
not scored. The rule is not fit to add.
