# fp17 screen: average trade size fails (2026-09-25)

The protocol was committed before this run (`b2ea8db`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp17/screen_2023.json`, sha256
`491e48f2fe3e4050a6e8ace01d892661a61106294644f67be73778c2537f0990`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| AVG-SIZE | 64 | +39.51 bps | +51.38 bps | +$25.28 | no |

The count clears 30, and the mean is positive. It does not beat the null.
The gross move is +59.61 bps; the 10 bps-a-side fill leaves +39.51. Every
fill matches fp5's `net_return` on that day's open. The daily grid has no
hole. The 2024-01-01 bar is an exit print and not a signal. The small-trade
tail is not scored. The 80th is not scored. The rule is not fit to add.
