# fp122 screen: put volume over call volume fails (2026-09-25)

The protocol was committed before this run (`502b701`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp122/screen_2023.json`, sha256
`614562ad80357e460d1844036075488aa9069258393b6a18938dc32211a6a8ab`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| PCVOL | 4 | −35.7949 bps | +180.9327 bps | −$1.4318 | no |

The count did not clear. The file's gross is −15.8465 bps and the file's
mean is −35.7949 bps. The file's null is +180.9327 bps. The total is
−$1.4318. The null is BTC's own one-day holds. STRIKE has the same null
because it also has 4 trades. The mean is negative, and 4 trades are
under 30. The count stays 30. The window stays 90 days and 90 prints.
Every fill matches fp5's `net_return`. 147 prints. 4 signal days, 4
trades. The reciprocal was not scored. Open-contract put over call is a
different rule; that one had no fill and does not count. The rule is not
fit to add.
