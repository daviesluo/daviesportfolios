# fp61 screen: the last hour's return fails (2026-09-25)

The protocol was committed before this run (`68b331f`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp61/screen_2023.json`, sha256
`2e03db8df457ab3d52f8546bf7124b8b7bae374ce4ea6147b7a277ee83eb55e8`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| LAST-HOUR | 43 | +58.92 bps | +66.23 bps | +$25.33 | no |

The count clears. The file's null is +66.2261 bps, the same figure as
§3.62, because both rules have 43 trades on the same BTC pool and the same
seed. The mean is positive and under the null. The gross move is +79.05
bps; the 10 bps-a-side fill leaves +58.92. Every fill matches fp5's
`net_return` on the next BTC open. All 43 signal days have a trade. The
hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC. That hole
is not the 23:00 hour, so the day stays a print. It was not filled in. 457
days have a last-hour return. The daily grid has 458 bars and no hole. A
weak last hour, and the 80th, are not scored. The rule is not fit to add.
