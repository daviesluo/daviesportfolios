# fp37 screen: BTC's return minus ETH's fails (2026-09-25)

The protocol was committed before this run (`1f9c4e5`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp37/screen_2023.json`, sha256
`172b3255fbec3a9a6dee71dd3fc9a63eea16ebc939de8fba6499e67839636b4a`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| REL | 44 | +18.75 bps | +63.98 bps | +$8.25 | no |

The count clears and the mean is positive. It loses to the null. The gross
move is +38.81 bps; the 10 bps-a-side fill leaves +18.75. Every fill matches
fp5's `net_return` on the next BTC open. All 44 signal days have a trade.
The null is the same figure as OPEN-VWAP's, because both rules have 44
trades on the same BTC daily pool and the same seed. BTC and ETH each have
458 daily bars and no hole. The first day has no prior close, so there are
456 differences. The 2024-01-01 close is set equal to the open on both
symbols. A day ETH outruns BTC is not scored. The 80th is not scored. The
rule is not fit to add.
