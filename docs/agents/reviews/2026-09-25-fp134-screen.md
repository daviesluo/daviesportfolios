# fp134 screen: the unspent-output range fails (2026-09-25)

The protocol was committed before this run (`a0e6d12`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp134/screen_2023.json`, sha256
`6fcd254882baeea1abd72292a0452d625574e451fccf0e3fdd788c3e817a44a3`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| UTXORNG | 75 | −1.3739 bps | +50.8989 bps | −$1.0305 | no |

The file's gross is +18.6433 bps and the file's mean is −1.3739 bps.
The file's null is +50.8989 bps. The total is −$1.0305. The null is BTC's
own one-day holds. The mean is negative and under the null. The window stays 90 days
and 90 prints. Every fill matches fp5's `net_return`. 457 prints, from
2022-10-01 through 2023-12-31, with no missing day. No point in 2024 was
stored. 76 signal days. The 2023-12-31 print would enter on 2024-01-01, outside the screen, so the file has 75 trades. The last sample was not the statistic. The raw samples are the same download as UTXO; the screen file is not. The rule is not fit to add.
