# fp118 screen: dispersion of the position ratio fails (2026-09-25)

The protocol was committed before this run (`e291a05`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp118/screen_2023.json`, sha256
`ceb87e5521f00cc71639d44d7eb882e3132d6f06b2ef14b711a9dd948d621094`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| TOPSTD | 11 | −60.8175 bps | +125.233 bps | −$6.6899 | no |

The count did not clear. The file's gross is −40.9192 bps and the file's
mean is −60.8175 bps. The file's null is +125.233 bps. The total is
−$6.6899. The null is BTC's own one-day holds. The mean is negative, and
11 trades are under 30. The count stays 30. The window stays 90 days and
90 prints. Every fill matches fp5's `net_return`. 376 prints, from
2022-12-15 through 2023-12-31. A day with a blank print, or fewer than two
prints, is not stored. Stored days carry 285 to 288 prints. The standard
deviation is computed at score time, not stored. 11 signal days, 11 trades.
No funding print was read. No hourly price bar was read. No alt quote was
read. The level of the same column is a different rule and was scored on
its own. A day below the 90th is not scored. The rule is not fit to add.
