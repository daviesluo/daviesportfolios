# fp95 screen: SOL quote over BTC quote fails (2026-09-25)

The protocol was committed before this run (`6b35a2c`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp95/screen_2023.json`, sha256
`121ac7a8df8ca6a1ecb1f7ea97757efe417a83da45f083446f7753f2a35c0baf`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| SOL-Q | 83 | +10.37 bps | +138.82 bps | +$8.60 | no |

The count cleared. The file's gross is +30.4074 bps and the file's mean is
+10.3667 bps. The file's null is +138.8212 bps. The total is +$8.6043. The
null is SOL's own one-day holds, so it does not match a BTC screen. The mean
is positive and under the null. Every fill matches fp5's `net_return` on the
next SOL open. 84 days cleared the 90th. The 2022-12-30 print enters on
2022-12-31, outside the screen, so 83 trades fill. The daily grid has 458
bars. The 2024-01-01 quote is stored as zero and is not a print, so 457 days
have a ratio. No hourly bar was read. ETH was not scored. A day SOL was quiet
versus BTC, and the 80th, are not scored. The rule is not fit to add.
