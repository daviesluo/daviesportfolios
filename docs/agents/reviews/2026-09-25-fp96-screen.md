# fp96 screen: BNB quote over BTC quote fails (2026-09-25)

The protocol was committed before this run (`6b35a2c`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp96/screen_2023.json`, sha256
`aabed6f68c8e1a94a6e339b799c925f26d2de7cda958262169b17415bcf23864`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| BNB-Q | 78 | −20.55 bps | +25.025 bps | −$16.03 | no |

The count cleared. The file's gross is −0.5662 bps and the file's mean is
−20.5451 bps. The file's null is +25.025 bps. The total is −$16.0252. The
null is BNB's own one-day holds, so it does not match a BTC screen. The mean
is negative. Every fill matches fp5's `net_return` on the next BNB open. All
78 signal days have a trade. The daily grid has 458 bars. The 2024-01-01
quote is stored as zero and is not a print, so 457 days have a ratio. No
hourly bar was read. ETH was not scored. A day BNB was quiet versus BTC, and
the 80th, are not scored. The sign is not flipped. The rule is not fit to add.
