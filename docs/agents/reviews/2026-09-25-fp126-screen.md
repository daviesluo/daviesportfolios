# fp126 screen: absolute-delta-weighted open contracts fails (2026-09-25)

The protocol was committed before this run (`502b701`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp126/screen_2023.json`, sha256
`d500e7398c1bef86f525dd6b95d557079f671e3dd3d1315678c6cab118e42c39`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| DELTA | 10 | −91.8685 bps | +117.1159 bps | −$9.1868 | no |

The count did not clear. The file's gross is −72.0324 bps and the file's
mean is −91.8685 bps. The file's null is +117.1159 bps. The total is
−$9.1868. The null is BTC's own one-day holds. The mean is negative, and
10 trades are under 30. The count stays 30. The window stays 90 days and
90 prints. Every fill matches fp5's `net_return`. 147 prints. The signed
sum was not scored. 10 signal days, 10 trades. The rule is not fit to add.
