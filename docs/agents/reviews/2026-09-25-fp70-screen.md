# fp70 screen: the close inside yesterday's range fails (2026-09-25)

The protocol was committed before this run (`0448ef6`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp70/screen_2023.json`, sha256
`f3153e2473bb1c379423e1937102fdf2673b92093002fa9003285e0fea6dc078`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| PRIOR-LOC | 43 | +41.45 bps | +66.23 bps | +$17.82 | no |

The count clears. The file's null is +66.2261 bps, the same figure as
§3.90, because both rules have 43 trades on the same BTC pool and the same
seed. The mean is positive and under the null. The gross move is +61.56
bps; the file's gross is +61.5561 and the file's mean is +41.4531. The 10
bps-a-side fill leaves +41.45. Every fill matches fp5's `net_return` on the
next BTC open. All 43 signal days have a trade. The daily grid has 458 bars
and no hole. The first day has no prior range, so there are 456 locations.
The 2024-01-01 high, low and close are stored equal to that bar's open,
which is an exit print only. A close that is not high in yesterday's range,
and the 80th, are not scored. The rule is not fit to add.
