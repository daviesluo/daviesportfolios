# fp123 screen: the count of strikes with open interest fails (2026-09-25)

The protocol was committed before this run (`502b701`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp123/screen_2023.json`, sha256
`905a361fe3d0ddd516bdb146b6641ec96213a60e9800ffb9a52506f5857dcfab`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| STRIKE | 4 | −141.2148 bps | +180.9327 bps | −$5.6486 | no |

The count did not clear. The file's gross is −121.4775 bps and the file's
mean is −141.2148 bps. The file's null is +180.9327 bps. The total is
−$5.6486. The null is BTC's own one-day holds. PCVOL has the same null
because it also has 4 trades. The mean is negative, and 4 trades are
under 30. The count stays 30. The window stays 90 days and 90 prints.
Every fill matches fp5's `net_return`. 147 prints. A call and a put at
the same strike count once. 4 signal days, 4 trades. The rule is not fit
to add.
