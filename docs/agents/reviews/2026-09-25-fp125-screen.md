# fp125 screen: open-contract-weighted days to expiry fails (2026-09-25)

The protocol was committed before this run (`502b701`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp125/screen_2023.json`, sha256
`48e59b25f9228aaf2b7b4c4b4d9edb939827a9a4fb204c80c40534a7ddfad706`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| DTE | 6 | +63.4634 bps | +166.5821 bps | +$3.8078 | no |

The count did not clear. The file's gross is +83.6105 bps and the file's
mean is +63.4634 bps. The file's null is +166.5821 bps. The total is
+$3.8078. The null is BTC's own one-day holds. The mean is positive and
under the null, and 6 trades are under 30. The count stays 30. The window
stays 90 days and 90 prints. Every fill matches fp5's `net_return`. 147
prints. The weight is open contracts, not premium. An unweighted mean was
not scored. 6 signal days, 6 trades. The rule is not fit to add.
