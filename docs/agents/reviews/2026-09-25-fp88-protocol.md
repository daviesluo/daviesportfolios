# fp88 protocol: how far the low held above the previous day's high (2026-09-25)

Written before any return of this rule was computed. The rules in fp5 through fp86 are dead, including LS-FADE, BODY and its
out-of-sample result, BAL-CHG, IMPACT and its out-of-sample result, PEAK,
STREAK, OPEN-DRIVE, AVG-CHG and its out-of-sample result, the screens fp30
through fp86, VOL-CLOCK, BASE-CHG, UNDER, HIGH-BASE, MID-RET, ETH-SPAN, CHOP,
CROSS, EXPAND, PRIOR-LOC, OPEN-IN, LOW-RET, PRE-QUOTE, MORNING, OPEN-X,
MAX-BAR, VWAP-OPEN, DIP, BUSY-RET, VWAP-LOC, MAX-GAP, NOON-QUOTE, HIGH-CLOSE,
WTD-RET, MED-OPEN and OVERLAP. AFTER-HIGH was scored and produced no fill.
It does not count, and the 90-print window is not widened to make it fire.
None of their cuts is reused, and no sign is flipped. A positive neighbour
is not a candidate. UP-STEP beat its null on 25 trades. CLOSE-HIGHS had 28
trades, LAST-EXT had 19, HIGH-BASE had 26, CROSS had 24 and EXPAND had 27.
PRE-QUOTE had 27, OPEN-X had 24, MAX-BAR had 28 and DIP had 26. MAX-GAP had
26 trades and OVERLAP had 23. The count stays 30, and those later years were
not pulled. A rule rejected without a scored run does not count. The count
of 30 is not lowered. The window is not shrunk.

This screen is the only measurement this file allows. A pass is not an
adoption. It earns a pre-registration before any later year is scored. 2023
is spent once this screen runs.

Nothing here changes a frozen spec, arms a row, or sends an order. The fill
is fp5's `net_return`. Public reads only. No bar from 2024 onward is stored
as a signal. The 2024-01-01 daily open may be an exit print. A later open
is a bug.

## Not this search

* Today's open over yesterday's close, minus 1. GAP-UP stays closed. The close is not stored.
* Where today's close sits in yesterday's range. PRIOR-LOC stays closed. The close is not stored.
* Where today's open sits in yesterday's range. OPEN-LOC stays closed.
* How far today's low undercut yesterday's close. UNDER stays closed.
* Today's low over yesterday's low, minus 1. LOW-LIFT stays closed. Today's high is not an input, and the denominator is yesterday's range.

## Costs, window, null

Ten basis points a side. No spread on this screen. Entries in
`[2023-01-01, 2024-01-01)` UTC. History for a threshold may start on
2022-10-01. The null is fp5's: 200 draws, seed `20250925`, index 190,
drawn from every in-screen BTC daily hold. A pass is at least 30 trades,
mean net above zero, and mean net above the null.

## The idea

**HOLD.** BTCUSDT. On day D, strictly before 2024-01-01, divide (today's low − yesterday's high) by (yesterday's high − yesterday's low). Yesterday's range must be strictly positive. A day with no previous day is not a print. Today's high is not an input. The close is not stored. The open is stored for the fill and is not an input. The statistic is known when D closes, and its timestamp is D.

It fires when the statistic is strictly above its own trailing-90-day 90th
percentile, at least 90 earlier prints in `[D−90 days, D)`, percentile
`sorted[floor(0.90 × (n − 1))]`. Enter the next BTCUSDT daily open. Hold
one day. A low that did not hold above yesterday's high is not a separate rule.

The 80th and the 95th percentiles of the same statistic are named as
neighbours. They are a veto that can only reject, and only after a
pre-registration. They are not scored on this screen.

## What a pass becomes

The house bar used for LS-FADE: both later sub-windows positive, above a
fresh null, positive with doubled costs, at least 30 trades, no month above
40% of the profit and the rest positive, and more than 4% a year on the $100
it locks, entries 2024-01-01 through 2026-09-24. 2023 is not part of that
bar. If this rule fails, it is dead and the next search does not inherit
the window or the cut. The 80th and the 95th of this statistic are a veto
that can only reject, and only after a pre-registration. They are not scored
on this screen.
