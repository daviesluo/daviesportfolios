# fp55 screen: the close over the day's low fails (2026-09-25)

The protocol was committed before this run (`68b331f`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp55/screen_2023.json`, sha256
`9f3afa15a40150ec9f26ba6feadbd692747d797606366ca7de1282938de76e2c`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| BOUNCE | 40 | +0.07 bps | +67.59 bps | +$0.03 | no |

The count clears. The file's mean net is +0.0707 bps and the total is
+$0.0283. The mean is positive and far under the null. The gross move is
+20.09 bps; the 10 bps-a-side fill leaves +0.07. Every fill matches fp5's
`net_return` on the next BTC open. All 40 signal days have a trade. The
daily grid has 458 bars and no hole. The window has 457 prints. The
2024-01-01 low and close are stored equal to that bar's open, which is an
exit print only. A close sitting on the low, and the 80th, are not scored.
The rule is not fit to add.
