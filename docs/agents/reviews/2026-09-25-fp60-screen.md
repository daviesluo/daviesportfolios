# fp60 screen: the open over yesterday's low fails (2026-09-25)

The protocol was committed before this run (`68b331f`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp60/screen_2023.json`, sha256
`02e1a3d5337d9ae173ac0464a51eb4fab8d2b0d88b38b2c27519e83da2c08514`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| OPEN-YLOW | 41 | +5.82 bps | +70.16 bps | +$2.39 | no |

The count clears. The file's null is +70.1636 bps, the same figure as
§3.63, because both rules have 41 trades on the same BTC pool and the same
seed. The mean is positive and under the null. The gross move is +25.86
bps; the 10 bps-a-side fill leaves +5.82. Every fill matches fp5's
`net_return` on the next BTC open. All 41 signal days have a trade. The
daily grid has 458 bars and no hole. The first day has no prior low, so
there are 456 distances. The 2024-01-01 low is stored equal to that bar's
open, which is an exit print only. An open that is not far above
yesterday's low, and the 80th, are not scored. The rule is not fit to add.
