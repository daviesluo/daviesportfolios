# fp127 screen: last mempool size fails (2026-09-25)

The protocol was committed before this run (`2470639`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp127/screen_2023.json`, sha256
`b8d0ae64bfeab5aefaca2c0505e83187b553bc80991669a19244ad1296019c4b`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| MEMBYTE | 99 | +35.5171 bps | +42.144 bps | +$35.162 | no |

The file's gross is +55.6082 bps and the file's mean is +35.5171 bps.
The file's null is +42.144 bps. The total is +$35.162. The null is BTC's
own one-day holds. The mean is positive and under the null. 99 trades
clear the count of 30 and still fail the null. The window stays 90 days
and 90 prints. Every fill matches fp5's `net_return`. 457 prints, from
2022-10-01 through 2023-12-31, with no missing day. The request ended at
2023-12-31 00:00 UTC. No point in 2024 was stored. The statistic is the
last sample of the UTC day, not the maximum and not the mean. 99 signal
days, 99 trades. Mempool transaction count was not read. The day's
maximum was not scored. The rule is not fit to add.
