# fp97 screen: XRP quote over BTC quote fails (2026-09-25)

The protocol was committed before this run (`6b35a2c`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp97/screen_2023.json`, sha256
`efee92d2641b53073c7197887c0aa832f39c1f265eacaec02f8ec783f4825e4f`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| XRP-Q | 57 | −51.765 bps | +139.13 bps | −$29.506 | no |

The count cleared. The file's gross is −31.8486 bps and the file's mean is
−51.765 bps. The file's null is +139.1256 bps. The total is −$29.506. The
null is XRP's own one-day holds, so it does not match a BTC screen. The mean
is negative. Every fill matches fp5's `net_return` on the next XRP open. All
57 signal days have a trade. The daily grid has 458 bars. The 2024-01-01
quote is stored as zero and is not a print, so 457 days have a ratio. No
hourly bar was read. ETH was not scored. A day XRP was quiet versus BTC, and
the 80th, are not scored. The sign is not flipped. The rule is not fit to add.
