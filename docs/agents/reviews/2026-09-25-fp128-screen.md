# fp128 screen: last unspent-output count fails (2026-09-25)

The protocol was committed before this run (`a0e6d12`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp128/screen_2023.json`, sha256
`ba9be1a1353f34ce568a9cbec0305b904a5317c28bcf88735fb16c2153b2bc77`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| UTXO | 264 | −2.5299 bps | +19.5105 bps | −$6.6789 | no |

The file's gross is +17.4851 bps and the file's mean is −2.5299 bps.
The file's null is +19.5105 bps. The total is −$6.6789. The null is BTC's
own one-day holds. The mean is negative and under the null. The window stays 90 days
and 90 prints. Every fill matches fp5's `net_return`. 457 prints, from
2022-10-01 through 2023-12-31, with no missing day. No point in 2024 was
stored. 265 signal days. The 2023-12-31 print would enter on 2024-01-01, outside the screen, so the file has 264 trades. The statistic is the last sample, not the maximum, the minimum, the mean, or the range. The rule is not fit to add.
