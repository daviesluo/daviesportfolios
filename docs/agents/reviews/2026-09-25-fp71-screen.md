# fp71 screen: the open inside today's range fails (2026-09-25)

The protocol was committed before this run (`6c35982`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp71/screen_2023.json`, sha256
`8da54b659696ea80e18685e8456c37503dc1115cd25ffa7eb134eb5ebdc5b714`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| OPEN-IN | 38 | −19.58 bps | +65.90 bps | −$7.44 | no |

The count clears. The file's gross is +0.4021 bps and the file's mean is
−19.5787 bps. The file's null is +65.8998 bps, the same figure as §3.83,
because both rules have 38 trades on the same BTC pool and the same seed.
The mean is negative and under the null. Every fill matches fp5's
`net_return` on the next BTC open. All 38 signal days have a trade. The
daily grid has 458 bars and no hole. The masked horizon is not a print, so
the window has 457 locations. The 2024-01-01 high and low are stored equal
to that bar's open, which is an exit print only. An open that is not high
in today's range, and the 80th, are not scored. The rule is not fit to add.
