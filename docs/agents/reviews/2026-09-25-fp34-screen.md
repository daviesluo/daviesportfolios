# fp34 screen: the rise in the trade count fails (2026-09-25)

The protocol was committed before this run (`1f9c4e5`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp34/screen_2023.json`, sha256
`1818f821a2e370d77a9c4575128289b7aa4c15888c2742b032a1650c4a54ab6b`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| TRD-CHG | 41 | +38.31 bps | +70.16 bps | +$15.71 | no |

The count clears and the mean is positive. It loses to the null. The gross
move is +58.40 bps; the 10 bps-a-side fill leaves +38.31. Every fill matches
fp5's `net_return` on the next day's open. All 41 signal days have a trade.
The daily grid has 458 bars and no hole. The first day has no prior count, so
there are 456 changes. The 2024-01-01 bar is an exit print, with its count
stored as 0. A day the count did not rise is not scored. The level of the
count is not scored. The 80th is not scored. The rule is not fit to add.
