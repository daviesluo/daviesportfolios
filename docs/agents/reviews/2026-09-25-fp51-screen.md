# fp51 screen: hours with a low above the open fails (2026-09-25)

The protocol was committed before this run (`169d5ce`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp51/screen_2023.json`, sha256
`5033156621d38d23575b0bd162b9c37990077e493f740cbdeefb69a16b2331a7`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| ABOVE-OPEN | 22 | +50.54 bps | +92.98 bps | +$11.12 | no |

The count is under 30 and stays there. The mean is positive and under the
null. The file's null p95 is +92.975 bps, the same figure as the clock-gap
screen, because both rules have 22 trades on the same BTC pool and the same
seed. The gross move is +70.66 bps; the 10 bps-a-side fill leaves +50.54.
Every fill matches fp5's `net_return` on the next BTC open. All 22 signal
days have a trade. The hourly grid has 10,967 bars and one hole, 2023-03-24
13:00 UTC, so that day is not a print. It was not filled in. The daily grid
has 458 bars and no hole. 456 days have a count. A day with fewer hours
above the open, and the 80th, are not scored. The rule is not fit to add.
