# fp56 screen: hours that break the running close fail (2026-09-25)

The protocol was committed before this run (`68b331f`). The one-element
close-field tuple was fixed in `96b48c5` before any bar was pulled. Two runs
of `measure.py` wrote the same file. Result
`docs/agents/backtests/fp56/screen_2023.json`, sha256
`0c2613ce1368bf11e589322ff8dc6dffee42b3beee2e4102d953f3de9b27108f`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| CLOSE-HIGHS | 28 | +54.86 bps | +84.17 bps | +$15.36 | no |

The count is under 30 and stays there. The file's mean net is +54.862 bps.
The mean is positive and under the null. The later years are not pulled.
The gross move is +74.99 bps; the 10 bps-a-side fill leaves +54.86. Every
fill matches fp5's `net_return` on the next BTC open. All 28 signal days
have a trade. The hourly grid has 10,967 bars and one hole, 2023-03-24
13:00 UTC, so that day is not a print. It was not filled in. The daily grid
has 458 bars and no hole. 456 days have a count. Fewer new closing highs,
and the 80th, are not scored. The rule is not fit to add.
