# fp91 screen: quote at the high over quote at the low fails (2026-09-25)

The protocol was committed before this run (`9614bb3`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp91/screen_2023.json`, sha256
`cefecbd707dfcc30a2122474a5918197767aa2108ab8ecef461c0579610879f6`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| HL-QUOTE | 30 | +3.22 bps | +75.19 bps | +$0.96 | no |

The count cleared. The file's gross is +23.2417 bps and the file's mean is
+3.2152 bps. The file's null is +75.189 bps, the same figure as §3.60,
because both rules have 30 trades on the same BTC pool and the same seed.
The mean is positive and under the null. The total is +$0.9646. Every fill
matches fp5's `net_return` on the next BTC open. All 30 signal days have a
trade. The hourly grid has 10,967 bars and one hole, 2023-03-24 13:00 UTC.
That day is not a print. It was not filled in. 456 days have a ratio. The
daily grid has 458 bars and no hole. A quieter high hour, and the 80th, are
not scored. The rule is not fit to add.
