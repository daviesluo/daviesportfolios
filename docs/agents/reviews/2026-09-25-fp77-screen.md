# fp77 screen: the day's VWAP over the open fails (2026-09-25)

The protocol was committed before this run (`6c35982`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp77/screen_2023.json`, sha256
`ad42d5620aa66278d5e6dac8e03d0fefc38743c433c18249d274341181ec3d45`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| VWAP-OPEN | 44 | −14.36 bps | +63.98 bps | −$6.32 | no |

The count clears. The file's mean is −14.359 bps and the file's null is
+63.9818 bps, the same null as §3.86, because both rules have 44 trades on
the same BTC pool and the same seed. The mean is negative and under the
null. The gross move is +5.63 bps. Every fill matches fp5's `net_return`
on the next BTC open. All 44 signal days have a trade. The daily grid has
458 bars and no hole. The masked horizon is not a print, so the window has
457 distances. The 2024-01-01 quote and base volume are stored as zero.
A VWAP that is not far above the open, and the 80th, are not scored. The
rule is not fit to add.
