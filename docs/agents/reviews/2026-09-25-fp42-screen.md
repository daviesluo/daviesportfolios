# fp42 screen: absolute move per unit of quote volume fails (2026-09-25)

The protocol was committed before this run (`ff4d925`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp42/screen_2023.json`, sha256
`9a330b9f3e051f0a713fcdfeb3a98f5baea5e96d919815e2fd64ba110a9f5a52`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| AMIHUD | 56 | +12.85 bps | +54.24 bps | +$7.20 | no |

The count clears and the mean is positive. It loses to the null. The gross
move is +32.90 bps; the 10 bps-a-side fill leaves +12.85. Every fill matches
fp5's `net_return` on the next BTC open. All 56 signal days have a trade.
The daily grid has 458 bars and no hole. Every day in the window has a
print, 457 of them. The 2024-01-01 close is stored equal to the open and
its quote volume is stored as 0. A quieter day, and the 80th, are not
scored. The rule is not fit to add.
