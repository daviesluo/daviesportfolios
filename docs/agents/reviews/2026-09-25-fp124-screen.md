# fp124 screen: gamma-weighted open contracts fails (2026-09-25)

The protocol was committed before this run (`502b701`). Two runs of
`measure.py` wrote the same file. Result
`docs/agents/backtests/fp124/screen_2023.json`, sha256
`df0bf189b20670b42c988ffbf6af1bf0dd05cdc4100d6404ec99525d07e09724`.

| rule | trades | mean net | null p95 | total on $100 | pass |
|---|---|---|---|---|---|
| GAMMA | 8 | −66.7907 bps | +143.3296 bps | −$5.3433 | no |

The count did not clear. The file's gross is −46.9044 bps and the file's
mean is −66.7907 bps. The file's null is +143.3296 bps. The total is
−$5.3433. The null is BTC's own one-day holds. The mean is negative, and
8 trades are under 30. The count stays 30. The window stays 90 days and
90 prints. Every fill matches fp5's `net_return`. 147 prints. A negative
gamma is not in the sum. No spot price was applied. 8 signal days, 8
trades. Vega and theta were not read. The rule is not fit to add.
