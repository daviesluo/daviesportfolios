name: land-once

# Retired. The scoreboard changes are on main (1ff1202) and the agents
# function has been deployed. This file no longer runs on push.
on:
  workflow_dispatch:

jobs:
  done:
    runs-on: ubuntu-latest
    steps:
      - run: echo "already landed"
