# Supabase Edge Function environment variables.
# Set these in Supabase dashboard → Edge Functions → Settings → Secrets.
# The browser does NOT read this file — it only sees src/supabase_config.js
# (anon key + URL, both safe to ship).
#
# `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are auto-injected by
# Supabase into every Edge Function's environment, so you only need to
# add the three app-level ones below.

# Long random string used to HMAC-sign auth tokens. Rotate to invalidate
# every existing session. Generate with:  openssl rand -hex 32
APP_AUTH_SECRET=replace-me-with-32-bytes-of-hex

# The two passwords gate the app. `?pwd=<APP_ADMIN_PASSWORD>` grants edit
# rights; `?pwd=<APP_RO_PASSWORD>` opens a read-only share view.
APP_ADMIN_PASSWORD=admin-password
APP_RO_PASSWORD=share-password

# Finnhub free-tier API key (https://finnhub.io/register, 60 calls /
# minute). Used by the `fundamentals` Edge Function for individual-stock
# P/E. Without it the P/E button stays hidden for stocks but everything
# else still works.
FINNHUB_API_KEY=replace-me

# Financial Modeling Prep free-tier API key (https://site.financial
# modelingprep.com/register, 250 calls / day). Used by the
# `fundamentals` Edge Function for index P/E (^GSPC / ^NDX / ^RUT via
# their SPY / QQQ / IWM ETF proxies — Finnhub's free tier doesn't
# carry ETF fundamentals so we need a second source). Without it the
# P/E button stays hidden for those three indices but stock P/E still
# works as long as FINNHUB_API_KEY is set.
FMP_API_KEY=replace-me
